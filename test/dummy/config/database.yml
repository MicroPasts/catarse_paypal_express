default: &default
  adapter: postgresql
  encoding: unicode
  host: localhost
  password:
  pool: 5
  username:

development:
  <<: *default
  database: catarse_paypal_express_development

test:
  <<: *default
  database: catarse_paypal_express_test
